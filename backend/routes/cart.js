const r=require("express").Router(),c=require("../controllers/cartController"),{auth}=require("../middleware/auth");r.get("/",auth,c.get);r.post("/",auth,c.add);r.put("/update",auth,c.update);r.put("/:productId",auth,(req,res,next)=>{req.body.productId=req.params.productId;next()},c.update);r.delete("/remove/:productId",auth,c.remove);r.delete("/:productId",auth,c.remove);r.delete("/clear",auth,c.clear);module.exports=r;
