const r=require("express").Router(),c=require("../controllers/adminController"),{auth,adminOnly}=require("../middleware/auth");r.use(auth,adminOnly);r.get("/dashboard",c.dashboard);r.get("/stats",c.dashboard);r.get("/products",c.products);r.get("/categories",c.categories);r.get("/orders",c.orders);r.get("/customers",c.customers);r.put("/orders/:id/status",c.updateOrder);r.delete("/customers/:id",c.deleteCustomer);module.exports=r;
